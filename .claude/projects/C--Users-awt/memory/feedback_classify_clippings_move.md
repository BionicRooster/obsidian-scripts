---
name: feedback-classify-clippings-move
description: Files in 10 - Clippings must be moved to 01/ subfolders after classification — no rule keeps them in Clippings
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 94946889-4bec-4d18-b8c9-cfbd948545f0
---

Always move classified files from `10 - Clippings\` to the appropriate `01/` subfolder. There is no rule that keeps files in `10 - Clippings` after classification.

**Why:** In a classify session, I invented a non-existent rule ("left in 10 - Clippings per rule") and failed to move 4 files that should have gone to `01/Bahá'í/`, `01/Home/`, and `01/PKM/`. The user had to correct this explicitly.

**How to apply:** The only files that stay in place after classify are:
- Vault root files (`*.md` directly in the vault root) — leave for user to manually review → `20 - Permanent Notes/`
- Files already in `02 - Working Projects\` or `03 - Completed Projects\`
- Files in `10 - Clippings\` that are Kindle Clippings (read-only, never modify or move)

All other `10 - Clippings\` files: classify, add nav + MOC link, then **move to the correct `01/` subfolder**.
